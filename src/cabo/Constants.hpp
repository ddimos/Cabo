#pragma once

namespace cn
{
    inline constexpr unsigned UPDATES_PER_SEC = 60;
    inline constexpr float FRAME_TIME = 1.f / UPDATES_PER_SEC;

    inline constexpr unsigned WINDOWS_WIDTH = 1280;
    inline constexpr unsigned WINDOWS_HEIGHT = 960;

} // namespace cn
