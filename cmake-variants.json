{
    "buildType": {
        "default": "debug",
        "description": "the build type to use",
        "choices": {
            "debug": {
                "short": "Debug",
                "long": "Emit debug information without performing optimizations",
                "buildType": "Debug"
            },
            "release": {
                "short": "Release",
                "long": "Enable optimizations, omit debug info",
                "buildType": "Release"
            }
        }
    },
    "launchTarget" : {
        "default": "Client",
        "description": "Launch target option (Client, Server)",
        "choices": {
            "Client": {
                "short": "Client",
                "long": "Client launch target",
                "settings": {
                    "LAUNCH_TARGET": "Client"
                }
            },
            "Server": {
                "short": "Server",
                "long": "Server launch target",
                "settings": {
                    "LAUNCH_TARGET": "Server"
                }
            }
        }
    }
}